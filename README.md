# app-finanzas-prog2
aplicación de gastos personales para programación 2


## Comandos utiles

`node --version` verificar la version de NodeJs  

`npm init` para inicializar el package.json en el repo  

`npm init -y` forma rapida de crear el package.json  

`npm install express` instalar la libreria Express  
